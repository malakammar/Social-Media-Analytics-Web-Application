# UserPortalManager
A C# web project for user authentication and validation, featuring post interaction tracking (likes, comments, shares), session and cookie management, and full user CRUD (create, read, update, delete) capabilities.
